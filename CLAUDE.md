# Repository instructions for Claude Code

## Commit attribution

When creating git commits in this repository, do NOT include a
`Claude-Session: https://claude.ai/code/...` line in the commit message.

The `Co-Authored-By: Claude ...` line may still be included, but the
session URL line must be omitted from every commit to every branch in
this repo.
